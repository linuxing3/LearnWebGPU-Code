  [project-build]
  command=xmake
  # set the working directory to the project root.
  cwd=$(VIM_ROOT)
  output=terminal
  
  [project-run]
  command=xmake run
  # <root> is an alias to `$(VIM_ROOT)`, a little easier to type.
  cwd=<root>
  output=terminal
